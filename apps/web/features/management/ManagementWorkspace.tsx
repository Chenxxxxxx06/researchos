'use client';

import { useQuery } from '@tanstack/react-query';
import { BookOpen, Building2, ExternalLink, FileText, FlaskConical, FolderKanban, Route, Settings, StickyNote, Users } from 'lucide-react';
import Link from 'next/link';
import { useState } from 'react';

import { Badge } from '@/components/ui/badge';
import { Skeleton } from '@/components/ui/skeleton';
import { getManagementSummary } from '@/lib/api/management';
import { useI18n } from '@/lib/i18n';

type Tab = 'researchers' | 'papers' | 'plans' | 'notes';

export function ManagementWorkspace({ projectId }: { projectId: string }) {
  const { locale } = useI18n();
  const zh = locale === 'zh-CN';
  const summary = useQuery({ queryKey: ['management-summary', projectId], queryFn: () => getManagementSummary(projectId) });
  const [tab, setTab] = useState<Tab>('researchers');
  if (summary.isLoading) return <div className="p-8"><Skeleton className="h-[34rem] w-full" /></div>;
  if (!summary.data) return <div className="p-8 text-sm text-danger">{summary.error instanceof Error ? summary.error.message : 'Could not load management data.'}</div>;
  const data = summary.data;
  const tabs: Array<{ id: Tab; label: string; count: number }> = [
    { id: 'researchers', label: zh ? '研究人员' : 'Researchers', count: data.counts.researchers },
    { id: 'papers', label: zh ? '文献' : 'Papers', count: data.counts.papers },
    { id: 'plans', label: zh ? '实验方案' : 'Experiment plans', count: data.counts.experiment_plans },
    { id: 'notes', label: zh ? '阅读笔记' : 'Reading notes', count: data.counts.reading_notes },
  ];
  return <div className="-m-6 min-h-[calc(100dvh-3.5rem)] bg-bg"><header className="border-b border-border bg-surface px-7 py-6"><Badge variant="neutral" size="sm">MANAGEMENT CENTER</Badge><div className="mt-4 flex flex-wrap items-end justify-between gap-4"><div><h1 className="text-3xl font-semibold tracking-[-0.035em] text-text">{zh ? '课题组与科研资产管理' : 'Lab and research asset management'}</h1><p className="mt-2 text-sm text-muted">{zh ? '同一权限模型下集中查看课题组、项目、人员、文献、实验方案和笔记；具体编辑入口保持与工作区一致。' : 'View the lab, project, people, papers, plans, and notes under one permission model, with direct links to their real editors.'}</p></div><Link href={`/projects/${projectId}/settings`} className="inline-flex h-10 items-center gap-2 rounded-md border border-border-strong bg-surface px-4 text-sm font-medium text-text hover:bg-surface-2"><Settings className="h-4 w-4" />{zh ? '成员与项目设置' : 'Member and project settings'}</Link></div><div className="mt-6 grid gap-3 md:grid-cols-2"><IdentityCard icon={Building2} label={zh ? '课题组 / 组织' : 'Lab / organization'} title={data.organization.name} detail={`${data.organization.slug} · ${data.organization.plan}`} /><IdentityCard icon={FolderKanban} label={zh ? '当前项目' : 'Current project'} title={data.project.name} detail={`${data.project.field ?? (zh ? '未设置领域' : 'No field')} · ${data.project.status}`} /></div><div className="mt-5 flex flex-wrap gap-7 border-l-2 border-accent/50 pl-5"><Metric icon={Route} label={zh ? '科研任务' : 'Missions'} value={data.counts.missions} /><Metric icon={Users} label={zh ? '研究人员' : 'Researchers'} value={data.counts.researchers} /><Metric icon={BookOpen} label={zh ? '文献' : 'Papers'} value={data.counts.papers} /><Metric icon={FlaskConical} label={zh ? '实验方案' : 'Plans'} value={data.counts.experiment_plans} /><Metric icon={StickyNote} label={zh ? '笔记' : 'Notes'} value={data.counts.reading_notes} /></div></header><div className="p-6 lg:p-8"><nav className="flex gap-1 border-b border-border">{tabs.map((item) => <button key={item.id} onClick={() => setTab(item.id)} className={`border-b-2 px-4 py-3 text-xs font-medium ${tab === item.id ? 'border-accent text-text' : 'border-transparent text-muted hover:text-text'}`}>{item.label}<span className="ml-2 rounded-full bg-surface-2 px-1.5 py-0.5 font-mono text-[9px]">{item.count}</span></button>)}</nav><section className="mt-5 overflow-hidden border border-border bg-surface">{tab === 'researchers' && <table className="w-full text-left text-xs"><Head values={[zh ? '人员' : 'Researcher', 'Email', zh ? '项目角色' : 'Project role', zh ? '状态' : 'Status']} /><tbody>{data.researchers.map((item) => <tr key={item.membership_id} className="border-t border-border"><Cell><p className="font-medium text-text">{item.display_name}</p></Cell><Cell>{item.email}</Cell><Cell><Badge size="sm" variant="info">{item.role}</Badge></Cell><Cell><Badge size="sm" variant={item.is_active ? 'success' : 'warn'}>{item.is_active ? (zh ? '有效' : 'Active') : (zh ? '停用' : 'Disabled')}</Badge></Cell></tr>)}</tbody></table>}{tab === 'papers' && <table className="w-full text-left text-xs"><Head values={[zh ? '文献' : 'Paper', zh ? '来源' : 'Source', zh ? '年份' : 'Year', zh ? '解析状态' : 'Ingest', '']} /><tbody>{data.papers.map((item) => <tr key={item.id} className="border-t border-border"><Cell><p className="max-w-2xl font-medium text-text">{item.title}</p><p className="mt-1 font-mono text-[9px] text-faint">{item.doi ?? item.id}</p></Cell><Cell>{item.source}</Cell><Cell>{item.year ?? '—'}</Cell><Cell><Badge size="sm" variant={item.ingest_status === 'completed' ? 'success' : 'neutral'}>{item.ingest_status}</Badge></Cell><Cell><Link href={`/projects/${projectId}/research/read/${item.id}`} className="text-info hover:underline"><ExternalLink className="h-3.5 w-3.5" /></Link></Cell></tr>)}</tbody></table>}{tab === 'plans' && <table className="w-full text-left text-xs"><Head values={[zh ? '方案' : 'Plan', zh ? '关联任务' : 'Mission', zh ? '状态' : 'Status', zh ? '版本' : 'Version', '']} /><tbody>{data.experiment_plans.map((item) => <tr key={item.id} className="border-t border-border"><Cell><p className="font-medium text-text">{item.title}</p></Cell><Cell>{item.mission_topic}</Cell><Cell><Badge size="sm" variant={item.status === 'published' ? 'success' : 'info'}>{item.status}</Badge></Cell><Cell>v{item.version}</Cell><Cell><Link href={`/projects/${projectId}/missions/${item.mission_id}/experiment-plan`} className="text-info hover:underline"><ExternalLink className="h-3.5 w-3.5" /></Link></Cell></tr>)}</tbody></table>}{tab === 'notes' && <table className="w-full text-left text-xs"><Head values={[zh ? '笔记' : 'Note', zh ? '论文' : 'Paper', zh ? '类型' : 'Type', zh ? '更新时间' : 'Updated', '']} /><tbody>{data.reading_notes.map((item) => <tr key={item.id} className="border-t border-border"><Cell><p className="max-w-xl line-clamp-2 text-text">{item.content}</p></Cell><Cell><p className="max-w-xs line-clamp-2">{item.paper_title}</p></Cell><Cell><Badge size="sm" variant="neutral">{item.note_type}</Badge></Cell><Cell>{new Date(item.updated_at).toLocaleDateString()}</Cell><Cell><Link href={`/projects/${projectId}/research/read/${item.paper_id}${item.mission_id ? `?mission=${item.mission_id}` : ''}`} className="text-info hover:underline"><ExternalLink className="h-3.5 w-3.5" /></Link></Cell></tr>)}</tbody></table>}</section><p className="mt-4 flex items-center gap-2 text-[10px] text-faint"><FileText className="h-3.5 w-3.5" />{zh ? '所有条目来自真实持久化对象；删除、编辑和发布继续使用对应工作区的权限与审计规则。' : 'Every row is a persisted object; edit, delete, and publish actions retain their workspace permissions and audit rules.'}</p></div></div>;
}

function IdentityCard({ icon: Icon, label, title, detail }: { icon: typeof Building2; label: string; title: string; detail: string }) { return <div className="border-l-2 border-border-strong bg-surface-2 p-4"><div className="flex items-center gap-2 text-[10px] text-faint"><Icon className="h-3.5 w-3.5" />{label}</div><p className="mt-2 text-sm font-semibold text-text">{title}</p><p className="mt-1 text-[10px] text-muted">{detail}</p></div>; }
function Metric({ icon: Icon, label, value }: { icon: typeof Route; label: string; value: number }) { return <div className="flex items-center gap-2"><Icon className="h-4 w-4 text-muted" /><div><p className="font-mono text-lg font-semibold text-text">{value}</p><p className="text-[9px] text-muted">{label}</p></div></div>; }
function Head({ values }: { values: string[] }) { return <thead className="bg-surface-2"><tr>{values.map((value, index) => <th key={index} className="px-4 py-3 text-[10px] font-semibold tracking-wide text-muted">{value}</th>)}</tr></thead>; }
function Cell({ children }: { children: React.ReactNode }) { return <td className="px-4 py-3 text-muted">{children}</td>; }
