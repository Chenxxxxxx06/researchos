{
  "name": "researchos-desktop",
  "version": "0.4.0",
  "private": true,
  "description": "ResearchOS Tauri desktop shell",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:portable": "tauri build --no-bundle",
    "validate": "node scripts/validate.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
